# VibeTyping — Project Map

> Auto-generated by the `project-map` skill (2026-06-06). Edit the prose sections
> freely; the module index between the markers is regenerated by `build`/`update`.

## Overview
VibeTyping is a macOS Input Method Extension that lets users dictate text into any application by pressing a configurable hotkey. The pipeline is: hotkey press → microphone capture (`Audio`) → on-device Whisper speech-to-text (`Transcription`) → local punctuation restoration (`Punctuation`) → optional LLM correction (`LLM`) → text committed via InputMethodKit (`InputMethod`). UI provides a live status overlay near the cursor and a SwiftUI settings window; configuration is persisted in `UserDefaults` via `Settings`.

## Entry points
- `VibeTyping/App/main.swift`

## Build / test
Open `VibeTyping.xcodeproj` in Xcode; build the `VibeTyping` scheme. The app must be installed as an Input Method (`System Settings → Keyboard → Input Sources`). No automated test suite exists — manual testing in a text editor is required.

## Conventions
- **Concurrency**: long-running work (model loading, audio capture, network) uses Swift structured concurrency (`async/await`, actors). UI updates always dispatched back to `MainActor`.
- **Shared singletons**: `WhisperKitManager.shared`, `PunctuationManager.shared`, `LLMClient.shared`, `AppSettings.shared` — all consumed by `VibeTypingInputController`.
- **Optional pipeline stages fail open**: punctuation and LLM correction both return the text they were given if their model or endpoint is unavailable, so a stage that breaks costs formatting, never the dictation.
- **InputMethodKit registration**: `@objc(VibeTypingInputController)` class name must match the `InputMethodConnectionName` key in `Info.plist`; changing either breaks IMK registration.
- **Silence threshold**: auto-stop fires from `SilenceDetector` via a callback into `AudioRecorder.startRecording(onSilenceDetected:)`, then marshalled to `DispatchQueue.main` by the caller in `InputMethod`.

<!-- projectmap:modules:start (generated — do not edit by hand) -->
## Modules (8) — grouping depth 2

| Module | Files | Doc | One-liner |
|---|---|---|---|
| `VibeTyping/App` | 3 | [doc](modules/VibeTyping__App.md) | Bootstrap: registers IMKServer, downloads/loads the recognition and punctuation models on launch |
| `VibeTyping/Audio` | 2 | [doc](modules/VibeTyping__Audio.md) | Microphone capture and silence-based auto-stop |
| `VibeTyping/InputMethod` | 2 | [doc](modules/VibeTyping__InputMethod.md) | Core IMK controller: hotkey → record → transcribe → punctuate → correct → commit pipeline |
| `VibeTyping/LLM` | 2 | [doc](modules/VibeTyping__LLM.md) | Optional OpenAI-compatible LLM post-correction of raw transcription |
| `VibeTyping/Punctuation` | 2 | [doc](modules/VibeTyping__Punctuation.md) | Local CT-Transformer punctuation restoration, with spacing repair |
| `VibeTyping/Settings` | 1 | [doc](modules/VibeTyping__Settings.md) | Singleton UserDefaults wrapper for all user configuration |
| `VibeTyping/Transcription` | 1 | [doc](modules/VibeTyping__Transcription.md) | WhisperKit model lifecycle management and speech-to-text |
| `VibeTyping/UI` | 3 | [doc](modules/VibeTyping__UI.md) | Status overlay, staged model download panel, and three-tab SwiftUI settings window |
<!-- projectmap:modules:end -->
